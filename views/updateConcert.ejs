<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Update Concert</title>
    <style>
        body {
            background-image: url('/images/concert_background.jpg');
        }
    </style>
</head>
<body>
    <%- include('partials/navbar.ejs') %>
    <div class="container mt-3">
        <h2>Update Concert</h2>
        <form action="/concert/<%= concert.id %>/update" method="POST">
            <div class="mb-3 mt-3">
                <label for="event_name">Concert Event:</label>
                <input type="text" class="form-control" id="event_name" value="<%= concert.event_name %>" name="event_name">
            </div>
            <div class="mb-3 mt-3">
                <label for="price">Price:</label>
                <input type="text" class="form-control" id="price" value="<%= concert.price %>" name="price">
            </div>
            <div>
                <label for="image">Current Image:</label><br>
                <input type="text" name="currentImage" value = <%=concert.image_path %> disabled><br>
                <img src = "/images/<%= concert.image_path %>" width="20%"><br><br>

                New Image:<br> <input type="file" id="image" name="image" accept="image/*" value=<%= concert.image_path %>><br><br>
            </div>
            <button type="submit" class="btn btn-primary">Update</button>
        </form>
    </div>
</body>
</html>
